import React from 'react';
import useTitle from './Home/Hooks/useTitle';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            
        </div>
    );
};

export default Blog;